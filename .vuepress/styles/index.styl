@require './palette.styl';

footer.footer {
    background: transparentify(darken($secondaryColor, 60%), 70%) !important;
}

header.home-hero {
    h1 {
        text-transform: uppercase;
    }

    h2 {
        font-family: sans-serif;
        border-bottom: none;
    }
}
